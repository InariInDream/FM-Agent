****************************************************************************
  不变式推理 before/after 演示 —— stream_processor（buffer 容量不变式）
  判断内容为大模型阅读代码后给出（离线录入回放，无需 API key）。
  Judgments were produced by an LLM reading the code, recorded and
  replayed offline; no API key needed.
****************************************************************************

============================================================================
场景 A（before）: buggy 代码 + 不含 invariants 的旧式 spec
============================================================================
  说明: 旧式 spec 没有 Invariants 段，reasoner 不会做逐段不变式检查；该函数没有显式 return，只剩最后一段『末尾隐式返回』的后条件检查，buffer 超限 bug 完全不可见。
  目标函数: stream_processor.py（37 行）
  spec 含 Invariants 段: 否（旧式 spec）
  切块结果（GRANULARITY=10）：共 3 块
    块 1: Line 1 ~ Line 10（10 行）
    块 2: Line 11 ~ Line 20（10 行）
    块 3: Line 21 ~ Line 37（17 行）
  --- 逐段检查过程（真实 reasoner 管线，判断来自大模型离线回放） ---
    [块 1] 生成块后条件（大模型判断，离线回放）: 初始化阶段：buffer 置为空列表，processed 与 dropped 计数器置 0，随后进入 while True 事件循环；此时 len(buffer) == 0，满足容量上限。
    [块 2] 生成块后条件（大模型判断，离线回放）: 本轮迭代从 source 拉取一个 batch：若 batch 为 None 则跳过本轮；否则将 batch 整体并入 buffer 并上报 buffer_len 指标。并入前未检查剩余容量，并入后 len(buffer) 等于原长度加 len(batch)。
    [块 3] 生成块后条件（大模型判断，离线回放）: 若收到 shutdown 请求则跳出循环；否则逐条弹出 buffer 中的元素处理：heartbeat 类型跳过，transform 抛 ValueError 时计入 dropped，其余结果写入 sink 并累加 processed；buffer 排空后更新指标。退出循环时 flush(buffer, sink) 后隐式返回。段内 buffer 只减不增，段结束时 len(buffer) == 0。
    [块 3] 后条件对照 spec（大模型判断）→ MATCH
  --- 结果 ---
  返回值（非 all_bugs 模式的字符串结果）:
    The function passes the verification. All code blocks satisfy the specification's post-condition.

============================================================================
场景 B（after）: buggy 代码 + 含 invariants 的 spec
============================================================================
  说明: spec 声明『任意时刻 len(buffer) <= CAPACITY』后，每段代码都要过一遍不变式检查；all_bugs=True 汇总全部违规。
  目标函数: stream_processor.py（37 行）
  spec 含 Invariants 段: 是
  切块结果（GRANULARITY=10）：共 3 块
    块 1: Line 1 ~ Line 10（10 行）
    块 2: Line 11 ~ Line 20（10 行）
    块 3: Line 21 ~ Line 37（17 行）
  --- 逐段检查过程（真实 reasoner 管线，判断来自大模型离线回放） ---
    [块 1] 生成块后条件（大模型判断，离线回放）: 初始化阶段：buffer 置为空列表，processed 与 dropped 计数器置 0，随后进入 while True 事件循环；此时 len(buffer) == 0，满足容量上限。
    [块 1] 不变式检查（大模型判断）→ 保持 MATCH
    [块 2] 生成块后条件（大模型判断，离线回放）: 本轮迭代从 source 拉取一个 batch：若 batch 为 None 则跳过本轮；否则将 batch 整体并入 buffer 并上报 buffer_len 指标。并入前未检查剩余容量，并入后 len(buffer) 等于原长度加 len(batch)。
    [块 2] 不变式检查（大模型判断）→ 违反 MISMATCH
    [块 3] 生成块后条件（大模型判断，离线回放）: 若收到 shutdown 请求则跳出循环；否则逐条弹出 buffer 中的元素处理：heartbeat 类型跳过，transform 抛 ValueError 时计入 dropped，其余结果写入 sink 并累加 processed；buffer 排空后更新指标。退出循环时 flush(buffer, sink) 后隐式返回。段内 buffer 只减不增，段结束时 len(buffer) == 0。
    [块 3] 后条件对照 spec（大模型判断）→ MATCH
    [块 3] 不变式检查（大模型判断）→ 保持 MATCH
  --- 结果 ---
  最终状态: MISMATCH
  违规汇总（all_bugs 模式，共 1 处）:
    - kind = invariant
      触发语句:
        Line 20:         buffer.extend(batch)
      原因: extend 前没有任何容量检查或排空操作。反例：buffer 中已有 5 条未处理元素时，source 返回 6 条元素的 batch，buffer.extend(batch) 后 len(buffer) 变为 11，超过 CAPACITY = 8，在随后逐条 pop 之前的窗口期内不变式被违反。

============================================================================
场景 C（after 对照）: fixed 代码 + 同一份含 invariants 的 spec
============================================================================
  说明: 修复版在 extend 前检查剩余容量并在空间不足时先 flush，逐段不变式检查全部通过。
  目标函数: stream_processor_fixed.py（36 行）
  spec 含 Invariants 段: 是
  切块结果（GRANULARITY=10）：共 3 块
    块 1: Line 1 ~ Line 10（10 行）
    块 2: Line 11 ~ Line 20（10 行）
    块 3: Line 21 ~ Line 36（16 行）
  --- 逐段检查过程（真实 reasoner 管线，判断来自大模型离线回放） ---
    [块 1] 生成块后条件（大模型判断，离线回放）: 初始化阶段：buffer 置为空列表，processed 与 dropped 计数器置 0，随后进入 while True 事件循环；此时 len(buffer) == 0，满足容量上限。
    [块 1] 不变式检查（大模型判断）→ 保持 MATCH
    [块 2] 生成块后条件（大模型判断，离线回放）: 本轮迭代从 source 拉取一个 batch：若 batch 为 None 则跳过本轮；否则先比较 batch 长度与 buffer 剩余容量，空间不足时先 flush 排空 buffer，再将 batch 并入并上报指标。并入后 len(buffer) 不超过 CAPACITY。
    [块 2] 不变式检查（大模型判断）→ 保持 MATCH
    [块 3] 生成块后条件（大模型判断，离线回放）: 若收到 shutdown 请求则跳出循环；否则逐条弹出 buffer 中的元素处理：heartbeat 类型跳过，transform 抛 ValueError 时计入 dropped，其余结果写入 sink 并累加 processed；buffer 排空后更新指标。退出循环时 flush(buffer, sink) 后隐式返回。段内 buffer 只减不增，段结束时 len(buffer) == 0。
    [块 3] 后条件对照 spec（大模型判断）→ MATCH
    [块 3] 不变式检查（大模型判断）→ 保持 MATCH
  --- 结果 ---
  最终状态: MATCH

